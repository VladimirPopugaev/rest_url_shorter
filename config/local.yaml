env: "local"
storage_path: "./storage/storage.db"
http_server:
  address: "localhost:8080"
  timeout: 4s
  idle_timeout: 60s
  user: "admin"
  password: "admin"
connection_db:
  username: "admin"
  password: "admin"
  db_name: "url_short_db"
  sslmode: "disable"
  address: "localhost:5432"
  max_pool_conn: 5

